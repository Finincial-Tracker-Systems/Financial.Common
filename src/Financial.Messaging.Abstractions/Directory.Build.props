<Project>
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />
  <PropertyGroup>
    <Version>1.0.2</Version>
    <Nullable>enable</Nullable>
    <Description>Core abstractions for messaging.</Description>
  </PropertyGroup>
</Project>